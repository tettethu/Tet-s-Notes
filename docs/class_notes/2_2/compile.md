# 形式语言与编译
## Recource
- [PKU: 编译原理实践](https://pku-minic.github.io/online-doc/#/)
- [哈工-编译原理公开课](https://www.bilibili.com/video/BV1dL4y1H7T8?spm_id_from=333.788.player.switch&vd_source=40cd9a3bec0b58d41925ce062c421cde&p=21)
- [交大门：往年题回忆](https://xjtu.app/t/topic/12940)

## Grade
平时30%
期末70%

---
## 1. 绪论
**这一章适合学完回来看**

**前端+中间表示+后端**
前端：对源代码进行分析，输出*中间表示形式*
后端：输入*中间表示形式*，进行*代码优化* 和 *目标代码生成*

**编译的过程划分**
1. 词法分析
2. 语法分析
3. 语义分析, 这前三步属于前端
4. 代码优化
5. 目标代码生成

## 2. 有穷自动机(FA)
> *Finite Automata, FA, 有穷自动机*

分为2类：DFA(确定型有穷自动机)，NFA(不确定型有穷自动机), 其中NFA还有变种$\epsilon-NFA$

接下来就是依次介绍这几种自动机
### DFA
$$
\begin{array}{l}
\{Q,\Sigma,v,q_0,F\}
\end{array}
$$
分别表示：状态集，输入符号集(字母表)，转移函数，初始状态，可接受状态集

- **扩展转移函数**：就是原来的转移函数参数为一个状态+一个输入符，扩展为接受一个状态+一个输入符号串, $\tilde{v}$
- **DFA的语言**: 一个输入符号串能让DFA走到可接受状态，就是DFA的语言，*所以这么看DFA其实就是用来规定一定规律的字符串的，比如以11为前缀的串，这样的DFA很好设计*

**Quiz**
- $Q\cap\Sigma$=__
- $F\_Q$
#### 表示形式
- 状态转换图：
	- 接受状态画双圈
	- 初始状态有一条没有出发节点和边权的边指向
- 状态转移表
	- 接受状态前面带`*`
- 记号
	- L(A): A的语言
	- {0,1}\*: 任意由01组成的串
直观来说，一个转移函数如下的DFA：

|                          | 0       | 1   |
| ------------------------ | ------- | --- |
| $\rightarrow q_\epsilon$ | $\perp$ | q1  |
| q1                       | $\perp$ | q11 |
| *q11                     | q11     | q11 |

尝试推一下会发现只有输入串前缀为11的时候能走到接受状态q11，这就是该DFA的语言

### NFA
对DFA，当确定一个状态+一个输入符号时，下一个状态是确定的。如果下一个状态不再确定，而是一个集合就是NFA

直观来说，就是状态转换图一个节点可以有几条出边的权相同了，那么对于一个输入串，在这个状态转移图上的状态转移路径就不再唯一，其中只要有一条路径能走到最终接受状态，就表示可以被接受，这种路径分叉的感觉，书上称之为*尝试*

概念
- **线索**：对于一个输入串，NFA每一条可能的状态转移路径，就是线索
### $\epsilon -NFA$
当一个状态甚至可以不消耗输入符号就转移到另一个状态的时候，就是$\epsilon-NFA$。*有一个明显的作用，如果构造一个初始状态q0，q0可以$\epsilon$转移到几个DFA的初始状态，就可以直接将几个DFA融合到一起*(相当于每一个输入串都会试图走各个DFA的线索看接不接受)

概念：
- **$\epsilon$闭包**: 一个状态仅靠$\epsilon$转移就可以抵达的状态集合/一个状态集合中各个状态的$\epsilon$闭包并集

### 考试题型
#### 画状态转移图
#### 归纳法证明/描述DFA的语言
#### NFA->DFA
总体思路就是将NFA中状态的集合作为DFA的一个状态，**可以形象的理解为可能的状态集合**。这样NFA就可以转换为DFA

具体来说，将{q0}作为初始状态，那么q0通过各输入符号能到达每一个状态集合也是新DFA的状态。对于NFA，每一个状态经过一个输入符号都可以到达一个状态集合，那么状态集合中的每一个元素经过同一个输入符号得到的各个状态集合的并集，就是一个状态集合经过一个输入符号所到达的新状态集合，将这两个状态集合作为两个状态，就可以转化成DFA中的一个状态到另一个状态的转移

一行一行写转移状态表，得到的新状态作为下面的新行开头状态，直到写完即可
#### $\epsilon-NFA$简化
概念：
- **单一路径**：除了始末节点，其他节点都只有单一的射入射出弧

优化方法：
1. 将$\epsilon$弧组成的单一路径改为从出发节点直接到结束节点的$\epsilon$弧
2. 如果单一路径的末端没有射出弧，并且始末节点都是接受/非接受状态，结束节点就可以直接合并到出发节点
3. 能$\epsilon$转移到接受状态的所有状态都成为接受状态

#### $\epsilon-NFA$ -> DFA
与NFA转化DFA相比，只是每计算出一个状态集合，需要再求一次$\epsilon$闭包而已，比如初始状态{q0}改成其$\epsilon$闭包

概念：
- **惰性求值技术**：似乎就是指每次计算一行，只考虑当前计算出来的状态的转移函数（这样才不会有无用状态）

#### 设计DFA
- 朴素的，先把输入符号集合写出来
- 推，每个状态有可能遇上哪些输入符，遇上之后是产生新的状态还是指向老状态
- tip：对于没有射出弧的接受状态，如果是很多条弧的终点，为了画图更好看，减少线之间的交叉，可以复制几份，编号不同但是实质相同，各个弧指向各自的终点，都是可接受状态
- tip：如果一个状态可能会有很多入边（即含义比较重要，可能是很多路径的中继，画到中间会好看一些）
- tip: 按照编号一个个画出边，不然容易遗漏, 或者做标记

## 3. 正则 RE
*和前面的DFA，NFA，$\epsilon$-NFA都是等价的*

**DFA->RE**
类似动态规划的递推式？
$R_{ij}^{(k)}$: 从节点 i 转移到节点 j 的输入符号串组成的正则表达式，且路径上的每个节点编号<=k
**NFA->RE**
- 状态消除法
	1. NFA->g-NFA: 初始状态无射入的弧，结束状态唯一且无射出的弧（显然通过加$\epsilon$弧就可以做到），例如3.2
	2. 逐步消除中间状态（记为q），如果有射入射出弧，那么将这条路径转化为一个权为正则表达式的弧，直接从射入节点指向射出节点
**RE->$\epsilon$-NFA**
对于每一个基本符号都转化为一条弧（直接路径），对于每一个运算符都转化为把已有的弧通过$\epsilon$弧连接起来

### 泵引理
*RE的必要条件，用于证伪*
一个RE，一定存在一个正整数n(可以看作RE对应的DFA的状态数量)，任何长度>=n的串，可以看作三个部分，满足
- 中间部分非空
- 去掉后面部分，长度<=n
- 中间部分去掉/重复任意次后，仍然属于RE的语言
*我的理解：简单来说，就是对于一个>=n的串，前n项组成的前缀有一个可重复可去掉的子串*

### DFA最小化
#### 填表算法
列为`Q[1:]`, 行为`Q[:-1]`, 形成一个下三角矩阵
1. 首先，接受状态和其余状态是可区分的，做标记
2. 一行一行，看通过同一个输入符号，一个状态对是否转移到了两个可区分状态上，是则标记
3. 重复，直到只剩下几个没有被标记的时候，逐个查看
4. 将等价的状态视作一个状态即可最小化
#### 霍普克罗夫特(Hopcroft)
概念：
- 划分，划分块
- 一致的划分块：对Q做划分，一个划分块中的每一个状态经过相同的输入符会转移到相同的新划分块
- 一致的极大划分块：划分块之间不能一致，就是说不能有两个划分块合并之后还是一致的，此时各个一致划分块就是最大的

算法
1. 可接受/不可接受是最初的划分
2. 写出当前各个状态经过各个输入符转移到的划分块，将本来在同一个划分块的总会转移到相同块的各个状态划到一起，不同的分开来（只分裂，不合并）
3. 不断对当前各个不一致的划分块分裂，直到得到各个一致的最大划分块

### 题型
- 描述/设计正则表达式
	- 描述的时候，主要抓住不带`*`的部分，这表示必须有的部分
	- 设计的时候，可以尝试划分为简单部分的连接
- DFA/NFA->RE
	- 状态消除法，消除的节点有m个射入节点，n个射出节点，那么一般补mn个弧
- 证明不是正则语言
	- 泵引理：就是先把n设出来，然后构造一个式子（一般是2n长的），证明他的前n项不存在可重复/可去掉的子串
- DFA最小化
## 4. 词法分析
*从源程序（字符串）到(词法)记号串*

### Concepts
- 词法单元：种属(词法类别)+个体，例如`<NUM,100>`, `<WHILE,_>`
	- 种属: 一个单词的种类，可以看作是一个正则语言(比如`整数`: `[+-]?[0-9]+)`, 程序中的词法单元有以下5个种属
		- 关键字/保留字: if/else/for/...
		- 标识符: 程序中定义的变量、类型、常量等
		- 常数
		- 运算符
		- 分隔符: `,`, `;`, `()`...
	- 个体：对于一个种属只有一个个体，记作`_`，否则记具体值比如`100`, 
	- 词法记号：`(WHILE,_)`是*一符一种*，`(KEY,while)`是*全体一种*
	
**书中提到的词法类别**：
*标识符*
- ID: 定义和c类似，开头为字母，后面可以是字母/数字，并且不能和关键字重名，只是不包含`_`了
*关键字/保留字*
- KEY: 关键字/保留字，是使用*全体一种*时的种属
- INT,FLOAT,VOID,IF,ELSE：关键字/保留字，是使用*一符一种*时的种属
*常数*
- NUM: NUMber
- FLO: FLOat number
*运算符*
-  AOP: Arithmetic OPerator, `{+,-,*,/}`: *全体一种*
	- ADD,SUB,MUL,DIV: *一符一种*
- ROP: Relational OPerator, `{<,>,<=,>=,==,!=`
- ASG: ASsiGnment, `{=}`
- AAS: Add ASsignment, `{+=}`
- AAA: Auto-Add Assignment, `{++}`
- BOP: Bool OPrator, $\{\lor,\land,!\}$
- DLI: DeLImeter, `{(,),[,],\{,\},\,,;}`
	- 分别对应LPA(Left Parentthesis),RPA,LBK(Left Bracket),RBK,LBR(Left Brace),RBR,CMA(Comma),SCO(Semicolon)

### 多语言联合DFA
$$
\sigma-DFA
$$
*POV*
- 语言：是串的集合，这里每一种词法类别都是一种正则语言，也就是一种语言
- DFA: DFA识别一种语言
- 对于一个DFA，我们可以判断一个串 属于/不属于 这个DFA的语言，那么有几个DFA的时候，我们人工很容易就可以判断一个串到底属于哪一个DFA/不属于任何一个DFA，只要每个DFA都走一遍，看能不能接受就行
- 为了避免有几个DFA都可以接受，就需要规定 *各DFA语言不相交*
- 那么，对机器来说，就需要把几个识别不同语言的DFA联合到一起，使得对于一个可能属于若干种词法类别的串，可以自动判断他到底属于哪一种语言，重点在于*如何联合到一起*
- 根据之前学过的 $\epsilon-NFA$，显然只要在开头加上一个 $\epsilon$转移，就可以合并为一个NFA，再转化为DFA就可以了，那么机器遍历各条线索，就可以判断出属于哪个DFA，即属于哪个语言
- 这个时候，显然根据接受状态的不同就可以判断一个串属于哪种语言
- 但是有个问题：对于一个串 `while1i++`，我识别出while以后，还要接着读入输入符，那么状态就会改变
- 所以引入*回退*操作，只要while对应的接受状态没有以1为权的出边，$\sigma-DFA$就会进入死亡状态，此时回退看上一步是不是走到接受状态就可以了
- 为了表示串的结束，引入一个符号 `#`，放在串的末尾
- 存在一种特殊情况，即一个语言本来是另一个语言的子集（比如 *关键字* 就是 *标识符*的子集），为了不相交将其从父集中挖掉，对于这种情况，子集者的 *优先级*较高，构建的时候先构建优先级高的FA，再构建优先级低的FA（比如说*标识符DFA*的可接受状态显然可以从*关键字DFA*的各可接受状态和中间状态转移得到，所以先构建*关键字DFA*会使得构建*标识符DFA*更简单）*图4-5是一个很好的例子*，于是按照构建FA的顺序就可以将原语言簇 $\mathcal{L}$转化为一个有序语言簇 $\mathbb{L}$

### Concepts
**Definition**
对书上定义的*说人话*解释
*对于定义4.1*，怎么解释都感觉这个定义很绕口
- *路径*：即输入串，以q为终点就是输入后得到的最终状态为q
- *L(q)*: 所有最终状态为可接受状态q的成功路径的集合，即输入串的集合，注意到语言也是输入串的集合，所以L(q)在这个**判别输入串属于哪种语言的任务**里， 就是指当DFA结束于q时，说明该串属于的语言，也就是说**每一个可接受的状态q，都代表了一个语言，记为L(q)**
- $\mathcal{L}$: 输入串集合的集合，可以看作就是语言的集合，所以叫语言簇，且要求**各语言两两不相交**
所以定义简单来说就是：
1. 一个二元组：DFA+语言簇（语言集合）
2. 语言簇中的语言不相交，显然如果相交我们就不能判断相交的部分中的串属于哪一个语言了
3. 每一个可接受状态所代表的语言一定是$\mathcal{L}$中某一个语言的子集，*就是当走到一个接受状态的时候，要有一个语言与之对应，表示识别出了这个串是这个语言的*
4. 每一个$\mathcal{L}$中的语言，至少包含了一个可接受状态代表的语言，*其实就是要有语言对应的接受状态*


**定理4.1**
对可接受状态集，可以做一个划分，划分后的每个部分都可以对应$\mathcal{L}(语言簇)$中的一个语言L
- 对应一个语言，意味着这个部分里面的各个q所代表的语言并起来就是L
- 也意味着划分数和语言簇上的语言数相同
- $\psi$: 一个满射(值域被覆盖): $F\rightarrow \mathcal{L}$，表示的就是可接受状态q和其对应的语言之间的关系

### TLDR
- $\sigma-DFA=(DFA\ A,\mathcal{L}), 语言簇\mathcal{L}不相交$
- 比DFA加上*回退*操作，使得可以连续识别一个串上各个词法单元，不会识别出一个词法单元后继续转移导致漏识别
- `#`表示串结束
- 一些书上的语言
	- udi: 无符号整数，其RE可以理解为: 不含前0的整数｜0
	- tail：小数部分，其RE可以理解为：不含后0的整数
	- udf：无符号浮点数，一定有一个`.`，两边至少有一边存在，前若存在为 `udi|0`(这个`|0`好像没有必要)，后若存在为 `tail|0`

### 题型
- 构建 $\sigma-DFA$
	0. 先按照一般的DFA设计方法设计试试，注意构建优先级，从优先级高的DFA到优先级低的DFA的转化。不行再试试下面的通法(感觉很麻烦)
	1. 从不同的初始状态出发，得到各个语言的FA
	2. 通过 $\epsilon$转移集合成NFA
	3. NFA->DFA(状态集做状态)得到 $\sigma-DFA$
- $\sigma-DFA$的工作过程
	- 和普通DFA一样读入输入符，转移状态
	- 当读入一个输入符会导致死亡时，回退一步，回退后在可接受状态上，就将之前读入的串记为一个词法单元，然后回到初始状态，开始识别下一个词法单元，否则报错

## 5. 上下文无关语言 (Context-Free-Language)
*CFL和之前的DFA，NFA，RE等不再等价*
### Basics
**Definition**
$(V(var),T(Terminate),P(Produce),S(Start))$
- V: 变元集合
- T：终结符集合，之所以称之为终结符是因为是语法树的叶子结点，即语法树终结于终结符
- P：产生式，形如`S->1S`，由此可以推出该CFG(Context-Free-Grammer)的CFL
- S：初始符号
**Concepts**
- 派生（书上是推导）：根据产生式左推右
	- *每一个变元可以看作对应一个语言(就是这个变元所能推出终结符串的集合)，整个CFG定义的语言就是初始符号S对应的语言*
- 规约：根据产生式右推左
- 文法符号串，终结符串
- 直接推导
- 最左推导
- 最右推导
- 句型，句子: 推导中的每一步结果都是句型，得到的终结符串就是句子
- 语法分析树
- 歧义性：存在多个语法分析树对应相同的句子，*定理：等价于存在两个相同的最左推导*，这可能是文法导致的歧义也有可能是语言本身的固有歧义性，例如$\{a^xb^yc^z|x=y \lor y=z\}$
	- 文法导致的歧义：
		- 左结合/右结合带来的歧义性, S->S+S|n文法显然对于1+2+3有两棵语法树，区别在于1+2/2+3谁是由一个S->S+S->n+n推导出来的结果，此时如果规定为左结合，那么右边的S应当先化为n，所以改成 $S\rightarrow S+\dot{S}|\dot{S},\ \dot{S}\rightarrow n$ 就可以解决结合性问题
		- 运算优先级的歧义性：E->0|1|E+E|ExE, `1+2*3`显然语法树不唯一，为此**需要给每个优先级安排一个变元**，高优先级变元的产生式不能含有低优先级变元。（这样考虑，初始符号肯定代表最低优先级+，T代表高优先级x，然后低优先级应当能直接推出高优先级的变元，因为E要能推导出E+E+E+E...型的式子，而其中的每一项都有可能是高优先级式子，然后最高优先级应当是()包裹的任意式子｜立即数
$$
\begin{array}{l}
E\rightarrow E+T|T \\
T\rightarrow T\times F | F\\
F\rightarrow (E)|d|i
\end{array}
$$
		- 奇奇怪怪的歧义，比如习题5.6，$S\rightarrow aS|aSbS|\epsilon$，aab有两个语法树
			- 歧义在于一开始采用两个非空产生式的任意一个都可以推出开头为a，且中间含有b的串
			- 思路：
				- 先看他表示的语言：终结符只有ab两种，接受的语言是
					1. 可以为空
					2. 非空则开头为a
					3. S->aSbS说明，b总是跟在某个a后面，中间和后面可以再任意派生 or 为空
					4. 所以总结就是：任意前缀，a的数量>=b的数量
					5. 想要消除歧义，考虑改造aS，让其表示只含a的式子，这样aSbS能否表示同时包含ab的所有式子呢，应当是可以的
					6. 于是得到
$$
\begin{array}{l}
S\rightarrow a\dot{S}|aSbS|\epsilon\\
\dot{S}\rightarrow a\dot{S}|\epsilon
\end{array}
$$
				- **错误的**参考答案是这样的：就是下一章对于回溯的修剪，但是这样只能得到LL(1)文法，而没有避免歧义
$$
\begin{array}{l}
S\rightarrow aST|\epsilon\\
T\rightarrow bS | \epsilon
\end{array}
$$
### CFG的化简
- 产生的：可以推导出终结符串的变元
- 可达的：从最初开始，能推导出含有X的串（由变元和终结符组成）


- 有用的：产生且可达的，*无用的即相反*
**化简的可靠顺序**
依次消除
1.  $\epsilon-产生式$, 记为 $\bar{e}$
	- 找出可空变元 **直接产生$\epsilon$的是可空变元，可以推导出$\epsilon$的也是可空变元**
	- 将产生式右端的可空变元Xi改为 `Xi | Xi取空之后的表达式`
2. 单元产生式, 记做 $\bar{u}$
	- 直接推导得到别的变元，改为直接推导到目标变元的直接推导结果
3. 非产生的无用符号, 记做 $g$
4. 非可达的无用符号, 记做 $a$
所以一般记为：$ag\bar{u}\bar{e}\cdot G$，放在后面的优先，右结合的

下一章讲了更详细的修剪✂️

### 题型
- 设计CFG
	- 5.1(2)，设计非ww形式的串的语言：奇数很好设计，偶数设计：两个中间元素不同的奇串放在一起，两个中心就会变成两半对应位置的元素！！！
- 消除歧义性
- CFG文法化简,
- 简单的证明？


---
进入编译的领域
[哈工大-编译原理公开课](https://www.bilibili.com/video/BV1dL4y1H7T8?spm_id_from=333.788.player.switch&vd_source=40cd9a3bec0b58d41925ce062c421cde&p=21)
## 6. 自上而下的语法分析
*课本居然被我扯下来一面，装订也是乱装订的嘛*

这一章和下一章讲的就是如何利用CFG对程序进行语法分析。

所谓*语法分析*，可以看作就是给定一个输入串(源程序)和一个CFG，看这个输入串能否被CFG接受，和DFA是类似的。不过看DFA是否接受只要按照DFA的定义逐步推导就能得到确定的结果，CFG的推导却有很多不确定性，选取不同的候选式和推导顺序，可以产生很多不同的推导过程和推导结果，所以我们一般会确定使用*最左推导(对应自上而下的语法分析)*/*最右推导(对应自下而上的语法分析)*，但哪怕当确定最左/右推导后，因为每个变元有很多候选式，推导流程依然不固定。选取*最左推导*，然后如何进行语法分析，这就是这一章要就解决的问题

**朴素的，可以利用回溯法进行语法分析**
*朴素的回溯算法*：最左推导会生成一个语法树，如果输入串能被推导出来，那么显然采用*深搜*的方法就可以搜索到*一个能推导出输入串的推导顺序*：如果将初始变元S看作根节点，将变元的各个候选式看作其各个子节点，这样得到的各个候选式子节点再采用其最左侧变元的各个候选式又会产生几个子节点，*于是推导的过程可以看作在语法树上进行搜索的过程*，当所有变元都变成终结符了就到了叶子节点，此时如果能匹配就成功，没能匹配就回溯。

有两个问题：
1. 回溯的方法效率很低
2. 对于*左递归文法*，即形如$S\rightarrow S\alpha|\beta$这样的式子，回溯法永远都不会停止

**解决方法就是修剪文法得到 LL(1)文法**
课程中还提到了S,Q文法作为得到LL(1)的过程，可以了解一下

*几个概念需要记清楚*
- SELECT集: 采用某个候选式的时候的当前终结符可以是什么
- FOLLOW集：可能跟在某个变元后面的终结符的集合
- FIRST集: 一个文法符号串的串首终结符集合。
$SELECT(A\rightarrow \epsilon)=FOLLOW(A)$

*对于这几个概念，可以这么理解*
假如推导到一半了，推导结果为$\alpha ABC$, 输入串是$\alpha b\gamma$，那么当前变元是A，当前终结符是$b$，

我们的目的是消除语法分析中的歧义性，当我们要推导A的时候，A如果存在多个产生式（候选式）$A->a C|b C$，我们就可以根据当前终结符=a/b来判断应该使用哪一个而省去回溯搜索的精力，能做到这一点的这就是*预测分析器*，在这里显然很好确认，应当采用$A->\beta C$，那么推广一下，对于一个产生式我们需要知道当前终结符为哪些的时候可以采用这个产生式，所以就有了SELECT集的概念：*即采用某个产生式的时候, 当前终结符可以是哪些，他们的集合就是SELECT集*，比如这里就是$SELECT\{A->\beta C\}=\{\beta \}$

*而FOLLOW集是相对于变元来说的*，就是说一个CFG中，初始符号推导所能得到的所有文法串中，某个变元A后面能跟哪些终结符，他们的集合就是FOLLOW集，对于文法串末尾的变元，则其FOLLOW集中加一个特殊终结符`$`(书上是`#`)

*FIRST集是相对文法符号串来说的*，是指一个文法符号串多能得到的所有终结符串中，每一个终结符串的首终结符组成的集合，*另外如果能推出$\epsilon$，则它也算是FIRST集的一部分*

*那么这几个概念有什么用处呢？*
还是回到我们的目的--消除歧义，而消除歧义其实就是要做到**一个变元的各个产生式，其SELECT集合不相交**，这样就做到了只看当前终结符就能确定应该使用哪一个产生式，而这样的文法就是**LL(1)**的

所以很好理解，对于$A->\alpha$：
- 当FIRST($\alpha$)不包含$\epsilon$的时候，其一定能推出终结符串，$SELECT(A->\alpha)=FIRST(\alpha)$
- 反之，则可能推出空串，那么 $\epsilon\in FIRST(\alpha),SELECT(A->\alpha)=(FIRST(\alpha)-\{\epsilon\})\cup FOLLOW\{A\}$(前者对应选择推出非空终结符串的情况，后者对应推空的情况，因为 $\alpha$是文法符号串，所以两者均有可能)

**LL(1)文法定义** 书上没有SELECT集的定义
那么一个LL(1)的文法，对于任何一个变元的各个候选式：$A\rightarrow \alpha|\beta$ 应当满足这样的条件：
1. 不是左递归的，可以带$\epsilon$
2. 各个候选式FIRST集一定不相交，很好理解
	1. 如果A不能推空，很直观，因为FIRST就是SELECT
	2. 如果A能推空，此时有且仅有一个候选式可以推空(即FIRST集包含$\epsilon$)，不然就无法唯一确定候选式，所以假如存在 $A\rightarrow \epsilon$那么其他候选式的FIRST集必然不包含$\epsilon$，或者如果一个候选式能推空，其他候选式一定不能推空
3. 如果A能推空，那么A的FIRST集和FOLLOW集不相交
	1. 很简单，我们要将A推空和A不推空的情况区分开来，FIRST集表示不推空，FOLLOW集表示推空。(FIRST(A)是各个候选式FIRST集的并，又因为各FIRST不相交，所以是各个FIRST的简单积累, 所以FIRST(A)和FOLLOW(A)不相交就意味着各个候选式的FIRST集也和FOLLOW(A)不相交，对于其中能推空的候选式，也是如此)
		1. 会不会存在限制过强的问题，假如 $\alpha\rightarrow a|\epsilon$，那么哪怕其FIRST集和FOLLOW(A)相交，此时也不会冲突，假如a为共同元素，当前终结符为a的时候这一步选择的候选式是确定的，肯定选 $A\rightarrow \alpha$
		2. 这意味着, $\alpha$的第一个符号必然不是a，甚至其中必然不包含任何终结符（因为能推空），所以当前终结符不会改变，还是a
		3. 这样一来就意味着，$\alpha$能推出a开头的终结符串，而推空后当前文法串还是能匹配a。这显然是两颗语法树，在这里也直接对应了两个候选式
		4. 所以**对于是可空文法串，其SELECT集的两个部分FIRST集和FOLLOW集都一定不能相交(FOLLOW集中只会包含终结符#，而不会包含 $\epsilon$)**
		5. 那么这和只检验SELECT集不相交是否等价呢？显然是等价的，因为对于可空文法串FIRST集和FOLLOW集相交的情况，此时SELECT集满足条件，但是之后的推导过程中一定会有一步SELECT集出现冲突，即出现一个候选式是推出相交终结符为首的终结符串，一个候选式能推空，这两个的SELECT集必然冲突


### 题型

**如何判定一个文法是不是LL(1)的，若不是则修剪**
0. 判断/消除左递归
	- 左递归分为直接左递归/间接左递归，顾名思义
	- 直接左递归很好消除：
		- 就是引入一个新变元和$\epsilon$，然后将左递推转化为右递归
		- 记做 $\bar{r}$
	- 间接左递归(通用消除方法)：
		- 给变元排个序，一般按照初始变元最后的顺序排
		- 不断将前面的变元产生式往后面代入，如果能代入就得到新的产生式，从而将可能存在的间接左递归转化为直接左递归
		- 消除产生的直接左递归
		- 直到不再能更新
		- 进行ag修剪(代入产生式很容易产生不可达变元)(不修剪空产生式&单元产生式)
		- 最后相当于得到了 $ag\bar{r} \cdot G$作为结果文法
1. 计算FIRST集
- 候选式的FIRST集就是首文法符号的FIRST集，当首文法符号能推出$\epsilon$的时候再加入下一个文法符号的FIRST集，终结符的FIRST集即为其本身，所以主要关注各个变元的FIRST集：
	- 看各个产生式
	- 产生式的FIRST集加入左元的FIRST集
	- 重复更新直到不能再更新
2. 根据FIRST集计算FOLLOW集
	- `#`加入FOLLOW(S)
	- 看各个产生式，从两个地方获取变元的FIRST集
		1. 对产生式中的各个变元，变元后面的文法符号串不为空，则将变元后面的文法符号串的FIRST集加入FOLLOW（除 $\epsilon$，其不属于任何FOLLOW集)
		2. 若该变元后面的文法符号串可以推出 $\epsilon$，则左元的FOLLOW加入该变元的FOLLOW
3. 判断，根据定义
4. 如何修剪
书上称之为p修剪，包含
- g：消除无产出变元, 即直接删去无产出变元和包含无产出变元的产生式
- a：消除不可达变元, 即直接删除不可达的变元，终结符，和包含他们的产生式
- $\bar{e}$: 消除 $\epsilon$产生式, 即删去产生$\epsilon$的产生式【例如$A\rightarrow \alpha|\epsilon$ -> $A\rightarrow \alpha$】，并将给各个包含这个变元的产生式加上其为$\epsilon$（即不包含这个变元）时的情况，【例如$B\rightarrow BA$ -> $B\rightarrow B|B\alpha$】
- $\bar{u}$: 消除单元产生式: 即将直接产生其他变元改为产生那个变元的各个产生式
- $\bar{r}$: 消除左递归, 方法为转化为右递归，$A\rightarrow A\alpha|\beta$ --> $A\rightarrow \beta\dot{A}, \dot{A}\rightarrow \alpha|\alpha \dot{A}$
- $\bar{c}$: 消除回溯, 这里应该是指解决公共前缀问题（只需引入一个新变元代替最大公共前缀后面的部分即可）, $A\rightarrow \alpha C|\alpha D$ --> $A\rightarrow \alpha\dot{A}, \dot{A}\rightarrow C|D$
- $\bar{a}$: 消除歧义性, 这里是说一个变元的候选式包含了其他变元的候选式？直接用被包含的变元替换即可了

书上的例子采用了三条路线
1. 划分子文法：大概就是某个子文法A可以单位产生别的子文法B里的变元，但是B的产生式不能再产生与A变元相关的串
2. 对各个子文法的问题进行修剪
3. 大致顺序为 $\bar{a}\bar{c}\bar{r}a\bar{u}$, 不过书上最后一步消除歧义性很奇怪，只是存在重复的变元，似乎不会带来歧义性，而且这种方式也没有一般性
**老师说修剪出来就行**

### 预测分析法
#### 递归的
- 每个变元可视为一个函数
- 这个函数根据当前终结符选择产生式，再调用下一级的变元
#### 非递归的 -- 下推自动机
- 构建*预测分析表*
- 使用栈实现非递归，从只有初始元素`#`开始
- 不断出栈->根据预测分析表选择产生式->入栈
- 如果栈顶是一个终结符，与当前终结符匹配，则出栈并取下一个终结符

## 7. 下推自动机 PDA
**定义**
PDA就是带栈的FA，可以是NFA/DFA

**定义**
相比于FA
- 多了关于栈的符号集 $\Gamma$、初始符号 $Z_0$
- 转移函数多了栈操作作为输入输出的一部分, $\delta$

**概念**
- 瞬时描述ID: (当前状态，剩余串，栈内容)
- 直接移动: **注意移动的符号，侧躺的T**
- 移动线索：对于一个确定的PDA和一个时刻的ID，显然可以推出其各条线索，查看是不是结束于**可接受ID**
- 关于PDA的接受，有两种方式
	- 以终结状态接受，类似FA，有一个/多个可接受状态f，能推出 $(f,\epsilon,\forall\alpha)$则为接受
	- 以空栈接受，状态不重要，输入串和栈内容同时空了就接受了
	- **显然这两种定义等价，并且可以很容易的互相转化**
		- 加一个初始/结束状态
		- 在之前的基础上加一个栈初始符号
		- 然后对于需要的状态加转移就行

**确定性PDA**就是对于确定的（当前状态，当前终结符，当前栈顶元素），有确定的移动状态
ppt定义, 对于任何一个状态q
- 如果没有不消耗任何符号的转移
	1. 如果q能消耗输入符号x和栈符号y转移，那么该转移唯一，并且也不能仅消耗x或仅消耗y转移，也就是说根据当前符号有唯一的转移，根据当前栈顶也有唯一的转移。
	2. 如果q能消耗输入符号x而不消耗栈符号转移，那么该转移唯一，并且不能再有消耗x和某个栈符号的转移
	3. 如果q能消耗栈符号y而不消耗输入符号转移，那么该转移唯一，并且不能再有消耗y和某个输入符号的转移
	4. **总结，就是对于任何一个状态，给定一个输入符号/栈符号都有唯一确定的转移，而不需要都给定**
- 如果有不消耗任何符号的转移，则不再有其他转移，且该转移唯一
**这样才能保证唯一确定, 不需要回溯搜索**
### 定理
- DPDA的语言包含正则语言，包含于CFL
- DPDA以终结方式接受的语言没有固有歧义性
- 任意一个DPDA，总存在一个无歧义的CFG 

### 题型
- 给PDA，串，写线索
- 设计PDA，两种接受方式
- 判断PDA确定性

## 8. 自下而上语法分析
就是与自上而下的语法分析相反，通过最右推导的逆过程*最左规约*将输入串规约成初始状态

**一个形象的流程是：** 不断将最左侧的终结符入栈，然后检测当前栈顶若干项是否构成某个产生式的右部，构成则进行规约，称之为*移入-规约语法分析器*, 书上的表8-1是一个很清楚的示例

**同自上而下一样，存在歧义性的问题**：自上而下是FIRST集不同时产生歧义，自下而上则是当多个产生式有相同后缀的时候会发生歧义

**于是引入了LR分析法**： 
*LR相比LL对文法的要求更低，接受的文法范围更广，并且避免了复杂化修剪*
- LR(k)分析: 对输入从左(L)到右扫描，反向推导出一个最右(R)推导序列，并且需要向前查看k个输入符号的LR分析

**一个LR分析的流程是这样的**：根据ACTION和GOTO函数，不断的读入输入串，改变状态，进行规约，详细的阐释需要先说明几个概念
- 状态：后面才说具体含义，这里可以理解为LR分析也可以看作使用一个下推自动机去读取输入串，这里的状态就是自动机的状态
- 栈：类似于下推自动机的栈，只不过PDA的栈存的是推导中间结果，这里的栈存的是规约的中间结果。且入栈的每一个终结符/变元都绑定着一个状态，一起出栈入栈。（或者也可以看作有两个栈：一个是已读入/已规约的文法符号的栈，一个是状态栈，这两个栈的元素是一一对应的，永远都要一起入栈出栈）
- ACTION函数：根据当前状态和读入终结符，判断此时是 
	1. 终结符入栈并入栈状态x(sx)
	2. 将栈顶若干个元素使用第x个产生式进行规约(rx)，状态栈顶状态出栈，再根据GOTO函数确定新状态入栈
- GOTO函数：在进行规约后，栈顶将会是一个变元，并且历史状态将会来到栈顶和栈顶变元给出此时是什么状态

一个形象的分析过程(对于给定的ACTION和GOTO)：

| 依据                                 | 栈    | 剩余输入 | 状态栈   |
| ---------------------------------- | ---- | ---- | ----- |
| 初始状态0                              | $    | bab$ | $0    |
| ACTION(0,b)=s4                     | $b   | ab$  | $04   |
| ACTION(4,a)=r3(B->b), GOTO(0,B)=2  | $B   | ab$  | $02   |
| ACTION(2,a)=s3                     | $Ba  | ab$  | $023  |
| ACTION(3,b)=s4                     | $Bab | b$   | $0234 |
| ACTION(4,b)=r3, GOTO(3,B)=6        | $BaB | b$   | $0236 |
| ACTION(6,b)=r2(B->aB), GOTO(2,B)=5 | $BB  | b$   | $025  |
| ACTION(5,b)=r1(S->BB), GOTO(0,S)=1 | $S   | $    | $0    |
| ACTION(1,$)=acc                    |      |      |       |

*LR(0)分析框架*
- 项目: $A->\alpha \cdot a \beta$, 可以理解为*正在尝试匹配 $A->\alpha a \beta$这个产生式，并且正在等待a*
	- 移进项目，待约项目，规约项目(完全项目)，后继项目
- 增广文法: 加一个 $S'->S$, 确保初始状态只有一个产生式
理论上可以将每一个项目看作一个状态，但是这样会有许多冗余的状态(*就是课本上的itemNFA*)，显然存在许多等价的状态，*将各个等价的状态组成的闭包作为一个状态*，这里的*等价*可以理解为遇到相同的输入符号时会转移到相同的下一个状态，*计算上*就是对于项目：$A->\alpha \cdot B \beta$将$B->\cdot \gamma$作为其等价项目，并类推出其他等价项目(*这就是课本上的itemDPDA*)
> LR(0)分析表计算
![img](/imgs/compile/LR(0)分析表计算.png)

根据状态转移图构建分析表的方式如下：
- 根据终结符转移状态, 则为ACTION项
- 根据变元转移状态, 则为GOTO项, 或者也可以看作都是GOTO的元素，只是将其中根据终结符转移的部分复制到了ACTION中
- 最后为项目集$\{S'->S\cdot\}$加上代表成功的ACTION(I,$)=acc

- 对于待约项目，ACTION结果为s
- 对于规约项目，ACTION结果为r

*冲突*
- 移进/规约 冲突: 当一个项目闭包中有以下两者时，若遇到当前输入终结符为a，则两条产生式均可以选择，发生冲突。*若要不发生冲突，则需要B的FOLLOW集中没有a*
	- $A->\alpha \cdot a\beta$
	- $B->\gamma \cdot$
- 规约/规约 冲突: 类似前面，*此时解决冲突需要B1，B2的FOLLOW集不相交*
	- $B1->\alpha\cdot$
	- $B2->\gamma \cdot$
当没有这两种情况时，就可以采用SLR分析法，建立SLR分析表

*SLR(1)分析框架*
- 和LR分析法的区别就在于建立分析表的过程中，如果ACTION的结果为r，还需要考虑FOLLOW集的约束
- 当SLR(1)的冲突消解规则消解不了 归约/移进冲突时，考虑通过定义先后顺序来消解：1）先规约表示左结合，2）先移进表示右结合
- 书上使用SLR进行语法分析时，似乎可以容忍左递归的语法, 并且由例8.4似乎可以接受 $\epsilon$表达式


*LR(1)分析法*
*这个课本上好像没有*
SLR只考虑规约的时候，当前输入终结符应当属于规约变元的FOLLOW集，而LR(1)在此基础上，提出对于不同的位置，采取同一个规约项目应当有不同的FOLLOW集，于是根据不同位置采用规约项目时不同的FOLLOW集又可以分裂出许多新的状态，相关概念修改为：
- 项目：$A->\alpha\cdot a\beta,b$（加上了一个b表示应当跟在A后面的终结符，是A FOLLOW集的元素/子集）,可选的b的集合是这个项目的*展望集*，*展望集*对于移进项目/待约项目在设计ACTION表/GOTO表的时候无需考虑
- 对于规约项目 $A->\alpha a\beta\cdot,b$ 则只有当前输入终结符为b时才能采用该规约
- 于是等价项目的推导方式为：
	- 对于 $A->\alpha\cdot B\beta,a$ 其等价项目为 $B->\cdot\gamma,b$ 而 $b\in FIRST(\beta a)$ 
	- 于是等价项目可以看作是一个待约项目/移进项目的子移进项目，即想要将某个移进项目转化成待约项目进而转化成规约项目时可能需要先进行的移进项目
	- 形象的理解，如果想要一步步将该$A->\alpha\cdot B\beta,a$转移到规约项目并规约，应当先通过$B->\cdot\gamma,b$规约出一个B来，所以前一个产生式决定了采取后一个产生式时规约得到B的位置，也就决定了后一个产生式的规约项目$B->\gamma\cdot,b$的展望集 $FIRST(\beta a)$

### 回归课本
概念：

- 最左规约：每次选取最左边的可规约串进行规约，其实就是LR在做的事情
- 规范推导：最右推导
- 规范句型：规范推导/规范规约得到的句型
- 规范规约：规范推导的逆，也是最左规约(最左规约不一定是规范规约，也就不一定是最右推导的逆)，*注意规范规约特有的箭头*
- 短语：一个句型 $\alpha\beta\delta$ 中的一个子串 $\beta$，可经过至少一步规约到某个变元A，就说这个子串 $\beta$是该句型相对于A的短语
- 直接短语：A直接推导出 $\beta$的时候
- 句柄： 假如有规范句型$\sigma Ax\xRightarrow*_{rm} \sigma\gamma x$(左式为规范句型，说明按照规范规约我们一定会规约到右式，从而进一步规约到左式) , 在这一步规范规约中$\gamma$就是句柄，可以理解为规范规约过程中，某一步的最左可规约串就是句柄, *规范规约是规约句柄的最左规约，规范规约每一步规约的就是句柄*
- 最大活前缀：规范句型后缀是句柄的前缀，*说人话就是到句柄为止的前缀*（包含句柄）
- 活前缀：最大活前缀的前缀(p)
	- 所以，SRF分析的时候，栈里的就是活前缀，当栈里的是一个最大活前缀的时候，就说明有规约项目(完全项目)了
- 文法项目：$e=(A,\alpha,\beta),即e=A\rightarrow \alpha\cdot \beta$
- 文法项目集：itemset
- 可用项目: 一个活前缀的可用项目，可以看作就是当前活前缀有可能规约的所有产生式对应该活前缀的项目（不看后面，只看已经移进的部分被活前缀包含了）
- 有效项目：考虑后面，确实能用该项目做规约，并且是规范规约，就说这个项目是有效项目
- itemNFA：将项目集作为状态集，那么显然是一个NFA
- itemDFA：如前面所得到的项目集作为一个状态，树上好像没有提等价项目的概念

#### 例8.4

| 剩余串    | 符号栈   | 状态栈   |                                         |
| ------ | ----- | ----- | --------------------------------------- |
| (())() | $     | 1     | shift                                   |
| ())()  | $(    | 14    | shift                                   |
| ))()   | $((   | 144   | shift                                   |
| ))()   | $((S  | 1445  | reduce($S\rightarrow \epsilon$),GOTO(5) |
| )()    | $((S) | 14456 | shift                                   |
| )()    | $(S   | 145   | reduce($S\rightarrow (S)$), GOTO(5)     |
| ()     | $(S)  | 1456  | shfit                                   |
| ()     | $S    | 12    | reduce($S\rightarrow (S)$), GOTO(2)     |
| )      | $S(   | 124   | shift                                   |
| )      | $S(S  | 1245  | reduce($S\rightarrow \epsilon$),GOTO(5) |
|        | $S(S) | 12456 | shift                                   |
|        | $SS   | 123   | reduce($S\rightarrow (S)$), GOTO(3)     |
|        | $S    | 12    | reduce($S\rightarrow SS\cdot$), GOTO(2) |
|        | $S'   | acc   | reduce($S'\rightarrow S\cdot$)          |

### 题型
- 判断规范句型
- 写短语、直接短语、句柄
- 证明SLR(1), LR(0)

## 9. 语义分析
*这一节似乎找不到相应的网课，只能硬啃，不过感觉这一章更讲人话了*
网上类似的资源关键词：*语法制导的语义分析*

**术语表**
- CFG G: $(\mathbb{V},T,\mathcal{P},S)$, 上下文无关语法
- att(G): CFG拓展成的属性文法，即给每一个产生式加上了一个*属性方程*
- 属性方程：描述左右元的属性之间的关系的方程
- 综合属性：属性方程依赖于自身及子节点
- 继承属性：在属性方程中依赖于自身、父节点、兄长节点(即在产生式中的出现顺序早于其节点的节点)

书上的例子9.3-5都挺好的

**如何阅读属性文法**
- 从右向左看，即从规约的角度看
- 属性方程其实就是赋值语句，是右求左
- 所以属性文法其实就是一般的CFG在规范规约（自下而上语法分析）过程中顺带调用各个产生式的属性方程，自下而上的求出各个变元的属性值/利用属性方程中函数的副作用产生一些效果【例如例9.5的计算器的display()】

### 符号表
template: 
```
func_name@table: (outer:outer_func_name@table
width:    argc:    arglist:    rtype:    level:    code:[]
entry:(name:    type:    offset:    )
entry:...
)
```
- 其中函数的level从1开始(最外层为0)
- entry.offset似乎是结束地址？说11章细说
- 最外层符号表的func_name为空
- 数组entry参数：`name,type,offset,dims,dim[0-dims-1],etype`
- func entry args: `name, type, offset, mytab`, size=8
- 作为函数形参的变量的类型
	- 数组：`ARRPTT`, size=4
	- 函数：`name, tyep:FUNPTT, offset:_+8, rtype`, size=8
- 一个函数声明/定义，有mytab，没有rtype，而作为函数形参的函数，没有mytab，有rtype
```
@table: (outer:NIL
...)
```

### 属性文法

### 语法制导的属性求值
本章的目的就是在语法分析的过程中顺带把语义分析给完成，所以是基于LL(1)/LR分析框架下的

**基于LL(1)的称之为LL制导的属性求值**
书上没讲，判定为不考，不过记个概念：
*L属性文法*：每个产生式中的各个变元，其属性方程都只依赖于前面的变元
*LL属性文法*：L属性文法+基础文法为LL(1)文法

**基于LR的称之为LR制导的属性求值**
要求：没有继承属性，只有综合属性（但是看例子好像只依赖兄长的继承属性也可以，或者说副作用涉及到了兄长属性没关系？）

书上提到了关于变量、函数、数组等的声明，但好像都比较泛泛而谈，不知道咋考

#### LR制导的属性求职过程
从LR的 符号栈 & 状态栈扩充了任意个*属性栈*，即每个属性一个栈，还是和符号栈一一对应，没有该属性的记做 `-`，*表9.1不错*

### 程序声明的语义分析

### 题型
题目出了两道：
1. 给定 $G_{(e)}$写出 $att(G_{(e)})$，而且就是写书上讲的挺详细的括号匹配；然后写出具体输入串的 *带注释语法树* 和 *语法制导的属性求值过程*; 至于归纳对应关系这个不太了解了
	1. 带注释语法树，就是在每个变元旁边标出求出的属性
	2. 属性求值过程，还是SLR(1)分析框架，不过省去了构建等价项目集的过程，直接对原串开干。形式上还是写成一个表格，每一列分别是符号栈(开头是#), 属性栈，剩余串，动作(移进/rx+属性方程求解)，然后一行一行求解
2. 利用主文法配套的属性文法，*翻译声明为符号表表示*，写出*翻译后的符号表的属性表表示*

### 习题9.3
**(1)**
$$
int\ x;float\ b[3,6];int\ foo(int\ x)\{int\ y; \hat{S}\};\hat{S}
$$
**(2)**
$$
int\ h(int\ f();int\ y;)\{int\ g(int\ c[];)\{\hat{S}\};\hat{S}\};\hat{S}
$$


## 10. 中间代码生成
讲的是要将高级语言代码翻译成*中间代码*，同语义分析，都是在语法分析的过程中进行翻译

作业没布置四元式, 暂且跳过

总的来说，是要将一个源程序规约成S，中间代码存放在S.code中

#### Basics: 中间代码
- 赋值语句直接搬
- 涉及运算一律转化为(临时)变量tx运算
- 数组访问按照类型计算出偏移量再访问
- 控制语句转化成`IF xxx THEN l1 ELSE l2` / `GOTO l3`这样的形式，介于汇编和高级语言之间

### 考点1：将高级语言语句翻译成三地址代码段
#### 1.1 翻译简单变量算数表达式赋值语句
可以分为两部分，一个是表达式的文法，一个是赋值的文法
**表达式文法**
$$
E\rightarrow i|f|d|E\mathcal{o}E|\mathcal{u}E|(E)
$$
**表达式文法的属性文法**
code属性：规约得到的E的对应的中间代码
place属性：规约得到的E对应的结果变量名
type属性：E对应的结果变量是INT/FLO

**一个简单例子**
$$
E\rightarrow i\quad\{t=newvar();E.place=t;E.code=gen[?t=?getv(i)],E.type=INT\}
$$
这里E代表由一个整数组成的表达式，假如其值为1，目前临时变量用到了t2, 将其归约为E后，其属性为`E.place=t2,E.code=[t2=1]`, 那么作为生成这一切的属性方程，显然我们需要先创建一个结果变量来存放这个整数`t=newvar()`, 于是自然结果变量`E.place=t`, 而对应的中间代码`t2=1`，其中的t2是t的实际变量名，1是i对应的结果，我们需要一个`gen[]`来做转换，`?t, ?getv(i)`理解为待转换为实际值

可以想象剩下几个产生式的属性方程怎么写
- E->d: 表示一个变量单独的表达式，x=getn(d)得到具体的变量名，`lookup(x)==UNBOUND`检查是否未定义，lookup(x,type)得到x的类型
- E->EoE: `p=getn(o);E.code=E[1].place p E[2].place` 错误，应为 `p=getn(o);E.code=gen[?E[1].place?p?E[2].place];E[0].code=E[1].code++E[2].code++E[0].code `

**赋值文法**
$$
S\rightarrow d=E
$$
这里S则只需要一个code属性，表示最终翻译为的中间代码，那就很简单了

#### 1.2 翻译下标变量算数表达式赋值语句
*首先我们要知道如何通过多维下标 得到一维下标*: `int a[d1,d2,d3...dn], a[i1,i2,...in]->a[t],t=(((i1*d2)+i2)*d3+i3)*d4...`

从而，我们可以将所有的下标变量`a[2],a[1,2,3],...`均视为一维的，只需要将下标作为一个单独的变元$\check{E}$, 并做处理即可

对于`arr[i,j,k]`，我们要将其翻译为最终得到`t2=arr[t1]`类型式子的程度，其中t1是所需元素相对与arr起始地址的相对偏移量，是需要$\times sizeof(type)$的, 所以逐步规约得到中间代码的过程其实就是逐步计算出t1的过程
*写几个例子*

#### 1.3 布尔表达式与控制语句
**布尔表达式 变元B**
- 属性tc：记录当这个表达式结果为真的时候应当转移到的标号LABEL(list)
- 属性fc：记录当这个表达式结果为假的时候应当转移到的标号LABEL(list)
- 属性code：这个表达式所翻译为的中间代码
- 一般将B翻译为中间代码：`[......IF xxx THEN ?tc ELSE ?fc]`，虽然这里似乎默认布尔表达式被用于控制语句，所以直接翻译为控制语句的中间代码，但是只有IF THEN ELSE，具体标号该放在哪里的`LABEL xxx`语句由具体的控制语句`if()...else...`/`while()`生成
*such as*: `B->a<1 { B.tc=(ll1),B.fc=(lnl1),B.code=[t1=1;IF a<t1 THEN ll1 ELSE lnl2] }`

**控制语句**
$$
\begin{array}{l}
&S\rightarrow if(B)S\ else S\\
&S\rightarrow if(B)S\\
&S\rightarrow while(B)S
\end{array}
$$
根据特点放置tc/fc并组织B.code，S.code即可了, 其中while语句还需要生成一个标号放在自己的代码前面，供转移

*另外，产生式中也可能有* `goto d`/`d:S`这样的式子，前者引用标号(可以先引用，后定义)，后者定义标号

*such as*
$$
\begin{array}{l}
\
&S\rightarrow if(B)S\ else\ S \\
&\{ \\
&\quad l=newl();\\
&\quad S[0].code=B.code++genl[lABEL\ ?B.tc]++S[1].code++gen[GOTO\ ?l]\\
&\quad\quad++genl[LABEL \ ?B.fc]++S[2].code++genl[LABEL\ ?l]\\
&\}
\end{array}
$$
### 考点2：对于给定代码段，给出带注释语法树作为翻译过程和结果的解释
- 记住几个变元，代码段S(code)，表达式E(place,code)，布尔表达式B(tc,fc,code)
- 记录最新的 t, l

### 考点3：给一个文法，设计一个属性文法将for翻译为三地址代码
*10.3*
```
S->for(S;B;S)S
{
	l=newl();
	S.code=S[1].code++genl[LABEL l]++B.code++genl[LABEL ?B.tc]++S[3].code++S[2].code++gen[GOTO l1]++genl[LABEL ?B.fc]
}
```

### 考点4：采用全局名的翻译样例
就是写出各级符号表`xxx@table`和各级中间代码`xxx@code`

> 采用全局名的翻译样例
![img](/imgs/compile/采用全局名的翻译样例.png)

## 11. 运行时环境
**真的很python**

一个函数的栈帧可以分为三个部分, 记为
f@frame:(<参数区><链接区><局部区>)
- 参数区：位于栈底侧，存放形参，传入变量则存放实参地址，传入常量则直接存放常量。最外层栈帧没有参数区
	- 按照参数从后向前顺序压栈(即维持参数从前向后地址增大的规律)，与中间代码顺序相同
	- 函数作为形参：存放地址，指向局部区为其开辟的2单位空间中存着函数入口label的那一位
	- 数组原型作为形参：放数组首地址
- 链接区：从底到顶为：访问链，控制链，反链
	- 访问链指向声明宿主的控制链, alink
	- 控制链指向引用宿主的控制链, clink
	- 反链不写, raddr
- 局部区：存放局部变量，传入的实参也要再分配一遍空间
	- **SP**：对于函数作为形参的情况，局部区存着被传入的函数的访问链（声明宿主）和代码入口f@label，参数区的对应位置就存放局部区存着入口那一位的地址
